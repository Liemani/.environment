#!/bin/bash
# print Wi-Fi IP address (works on macOS and Linux)

if command -v networksetup >/dev/null 2>&1; then
    # macOS
    networksetup -getinfo Wi-Fi | grep "^IP address: "
else
    # Linux / Ubuntu
    hostname -I
fi
