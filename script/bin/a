#!/bin/bash
# source a_script

if [ $# -eq 0 ]; then
  echo 'usage: a <a_script> [<args>]'
  . a ls
  return 1
fi

if . "$A_CORE/_get_a_script" "$1"; then
  shift
  . "$_a_script" "$@"
else
  echo "a: $1: No such file" >&2
fi
